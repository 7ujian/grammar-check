{
  "description": "When user submits a prompt, check for English grammar issues via Claude API and inject correction context",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 ${CLAUDE_PLUGIN_ROOT}/scripts/grammar-check.py",
            "timeout": 20
          }
        ]
      }
    ]
  }
}
